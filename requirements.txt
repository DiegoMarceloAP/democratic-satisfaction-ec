# Fase 2 (Preparación de datos)
pandas>=2.0
numpy>=1.24
pyreadstat>=1.2               # lee los .sav (SPSS) de Latinobarómetro 2011-2024.

# Fase de Modelado
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
imbalanced-learn>=0.12      # SMOTE (solo sobre X_train, después del split)
pytorch-tabnet>=4.1
torch>=2.1                  # CNN-LSTM y TabNet; instalar la variante CUDA si hay GPU NVIDIA disponible
shap>=0.45                  # XAI: TreeSHAP (XGBoost/LightGBM) y KernelSHAP (MLP/CNN-LSTM)

# Notebooks y reportes
jupyter>=1.0
matplotlib>=3.8
seaborn>=0.13

# Extracción (scripts/extraer_enemdu_superset.py)
requests>=2.31
